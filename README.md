# exposition
Code Samples for Blog

Usually every folder will have `run_fast.sh`, `run_full.sh` and `plot_results.sh` scripts to help run the benchmarks. 
This also requires that miniconda is already installed since we will download a compiler in the miniconda environment, to contain the dependencies.